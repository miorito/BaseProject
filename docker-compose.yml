services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: production
      PORT: 3001
      DATABASE_PATH: /app/server/data/app.db
    volumes:
      - app-data:/app/server/data

volumes:
  app-data:
